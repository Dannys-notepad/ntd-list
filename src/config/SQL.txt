CREATE TABLE users(
  uid, VARCHAR(10) NOT NULL PRIMARY KEY,
  ip VARCHAR(50) NOT NULL UNIQUE,
  date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE notToDos(
  title VARCHAR(50),
  content TEXT,
  date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (uid) REFERENCES users(uid)
);